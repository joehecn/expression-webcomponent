
import { expression } from 'mathjs'
import '../src/expression-webcomponent.js'

export default {
  component: 'expression-webcomponent',
  title: 'ExpressionWebcomponent',
  tags: ['autodocs'],
}

export const Default = {
  args: {
    expression: '1 + 2',
  }
}
